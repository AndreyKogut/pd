import { WizardAnchorDirective } from './wizard-anchor.directive';

describe('WizardAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
